rootProject.name = 'DialogueLib'

var dirApi = file('../../java/api/')
if (dirApi.exists()) {
    include(':api')
    project(':api').projectDir = dirApi
}

var dirCmdr = file('../../java/cmdr/')
if (dirCmdr.exists()) {
    include(':cmdr')
    project(':cmdr').projectDir = dirCmdr
}
var dirCmdrSpigot = file('../../java/cmdr/spigot/')
if (dirCmdrSpigot.exists()) {
    include(':cmdr-spigot')
    project(':cmdr-spigot').projectDir = dirCmdrSpigot
}
